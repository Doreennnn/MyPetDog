extends Node3D

# The key required to unlock the object (exported for easy assignment in the editor)
@export var key: StaticBody3D  

# Tracks whether the object is locked or not (exported for easy assignment in the editor)
@export var locked: bool
